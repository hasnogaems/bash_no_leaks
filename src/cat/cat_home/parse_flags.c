#include <stdio.h>
#include <stdio.h>
#include <getopt.h>
#include "headers.h"
Flags parse_flag(int argc,char *argv[], Flags flags)
{
    int opt;
    
    while(0){
        opt=getopt_long(argc, argv, "+bse", 0);

}
if (opt==-1) break;

switch(opt){
    case('b')
}

//sourcetree
}